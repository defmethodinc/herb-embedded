
# Ruby / Bundler
/.bundle/
/pkg/
*.gem
.yardoc/
/doc/
/coverage/

# JS build tooling (Node is required to build the vendored bundle, never to use it)
/node_modules/
/vendor/*.tmp

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*
